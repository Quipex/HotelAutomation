{
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "17.3.0"
  },
  "scripts": {
    "lint:js": "npx eslint \"src/**/*.ts\" --fix",
    "lint": "npm run lint:js",
    "build:ts": "tsc && tscpaths -p tsconfig.json -s ./src -o ./dist",
    "build": "npm run build:ts && cp package.json dist",
    "start": "node ./dist/index.js"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.4",
    "tscpaths": "0.0.9",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "moment": "^2.29.1",
    "uuid": "^8.3.2"
  }
}
